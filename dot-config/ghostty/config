theme = Catppuccin Mocha

window-width = 128
window-height = 36
window-decoration = server

# improve startup time of additional terminals if one instance is already open
# also no confirm to close, even if process is running within the terminal
gtk-single-instance = desktop
confirm-close-surface = false

font-family = "FiraCode Nerd Font"
font-style = Regular
font-style-bold = Bold
font-style-italic = Italic
font-style-bold-italic = Bold Italic
font-size = 11

shell-integration = bash
shell-integration-features = no-cursor,no-title,ssh-env
cursor-style = block

background-opacity = 0.69
background-blur = 69
